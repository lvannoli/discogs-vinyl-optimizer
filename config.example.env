# Optional. Keep the real value in .env, which is ignored by git.
DISCOGS_USER_TOKEN=

# Optional email delivery for discogs_shortcut.py --email-results.
DISCOGS_EMAIL_TO=leonardo.vannoli@gmail.com
SMTP_HOST=
SMTP_PORT=587
SMTP_FROM=
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_STARTTLS=true
